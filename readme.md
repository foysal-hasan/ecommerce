# To run go to /frontend/src/constants.js and replace

- export const baseUrl = "";
- export const baseUrl = "http://localhost:5000";

# And Create .env file in the root put values for all .env.examples
